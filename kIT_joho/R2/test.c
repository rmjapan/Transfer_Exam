/**
*    created by: shosei
*    13.05.2022 22:47:56
**/
#include<stdio.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
#define pra(ans) printf("%d\n", ans);

typedef struct {
  double x,y;
}Point;

void koch(int n,Point a, Point b){
}


int main()
{
  int n;
  scanf("%d", &n);
  Point a, b;
  a = {0.0, 0.0};
  b = {100.0, 0.0};

  printf("%lf", a.x);
  //koch(n, {0, 0}, {100, 0});
  return 0;
}